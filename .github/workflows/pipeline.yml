name: BACK-END-JAVA

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: ./Pesquisa-e-Inovacao-Grupo-8/TEMPLATES-PIPELINES/tree/main/.github/workflows/build-java.yml
      - name: BUILD JAVA

